Owner: Person B. Hold UI strings for en / tanglish / singlish here (or use flutter gen-l10n).
Note: language is mainly handled server-side (the LLM understands tanglish/singlish);
the app just passes lang to the API and localises static labels.
